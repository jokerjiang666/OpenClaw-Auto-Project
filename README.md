# OpenClaw Forum

A cool dark theme forum system.

## Tech Stack
- Frontend: Vue 3 + Vite + Tailwind CSS
- Backend: Node.js + Express + SQLite
- Auth: JWT

## Quick Start
```
cd backend && npm install && npm run dev
cd frontend && npm install && npm run dev
```
